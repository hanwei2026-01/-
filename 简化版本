1. 模型目的

在已知 总能量 / 总功率​ 的情况下，

按几何规则对空间区域进行能量分配。

本模型：

不是物理理论

不替代波动光学 / 几何光学

只提供 可计算的分配规则

2. 坐标系与约定

原点：发射口

X 轴：主传播方向

Y 轴：横向

有效区域：X > 0

X = 0 仅作为参考面

可使用二维截面 + 镜像对称。

3. 参数说明

名称

	

含义

	

约束




theta

	

扩散半角

	

0 < theta < 90°




theta_full

	

扩散全角

	

必须小于 180°




K

	

斜率参数

	

K ≈ tan(theta)




N0

	

边界修正

	

近场可设为 0

重要限制：

当扩散全角 >= 180° 时，本模型不适用

应使用球面或近各向同性模型

4. 扩散边界定义

单侧面积扩散边界表示为：

f(x) = (K + N0) * x

含义：

描述位置 x 处的可到达范围

不描述光的传播过程

可作为基础形式，后续可扩展为变参数或曲线

5. 权重与能量分配规则
5.1 权重定义

权重是 相对分配因子

不是概率密度

不要求天然归一化

对任意区间 i：

weight_raw[i] = geometry_measure[i]

其中 geometry_measure 由几何规则给出（如反函数导数的覆盖区域）。

5.2 能量分配

给定总能量 E_total：

E[i] = E_total * weight_raw[i] / sum(weight_raw)
6. 近场示例（N0 = 0）

扩散边界：

f(x) = K * x

反函数关系：

x = y / K

反函数导数：

dx/dy = 1 / K

局部几何度量：

weight_raw(X0) = X0

能量分配：

E(X0) = E_total * X0 / sum(Xj)
7. 适用范围与失效条件
适用场景

有明显主传播方向

扩散全角 < 180°

X > 0

不适用场景

近各向同性辐射

全角 >= 180°

需要严格波动 / 衍射描述的情形

8. 工程使用提示（重要）

本模型只负责 “怎么分”

总能量 / 总功率必须由外部输入

归一化是后处理步骤，不是模型内部约束
