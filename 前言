一、背景与动机

传统波动光学与几何光学的经典描述，往往在计算过程中隐式或显式地忽略了光的传播方向性、扩散特性以及能量权重与流量的空间分布，将问题简化为相位演化或理想射线模型。这种做法虽然在理论推导上具有高度普适性，但在面向实际工程问题时，却因缺乏对局部能量、流量与方向约束的直接刻画，给工程计算与应用带来了诸多不便与阻碍。

为此，本文（本章）从工程可实现性与可计算性出发，提出一种面向特定区域的能量与流量近似描述方法。该方法以角度参数 K 为核心，结合小角条件下的斜率函数与三角形近似，显式引入方向、扩散范围与起点边界，构建适用于工程化分析的近似方程，用以描述光在给定区域内的能量权重与流量分布。

已知光具有扩散角度，但其能量在空间中的分布并非均匀，通常表现为中间权重大、周边权重小的特征。针对这一分布特性，本文提出如下理论框架：以扩散角度为基础，引入方向权重与边界约束，通过斜率函数与近似几何模型（如三角形表征），构建适用于工程计算的能量与流量分布方程，从而在简化计算的同时，保留光在局部区域内的权重差异与方向特性。

二、坐标系、对称性及模型约化（原说明保留）

在具体建模中，取发射口为坐标原点（零点），沿主传播方向建立坐标轴。观察不同位置处的光场分布可以发现，其扩散截面均呈现为圆形或近似圆形，表明在传播过程的各个阶段，光的扩散行为具有稳定的方向对称性。对应到本文所定义的参数 K，即在整个传播路径上，K 随位置呈稳定、连续的变化规律，既不出现突变，也不依赖于观测距离的非单调修正。这一特性为采用单一斜率函数描述扩散角度与能量权重的空间演化提供了物理依据。

在实际建模中，为降低复杂度，将三维光场简化为二维平面分布，并进一步利用镜像对称特性，取其中间截面作为代表性平面。在此基础上，引入关于 X 轴的对称扩散假设，将光的立体发散过程等价为一维平面模型中的 X 轴方向对称扩散。该模型要求：平面分布下的扩散行为在 X 轴两侧保持对称，且其一维特征能够与完整立体分布中的对应方向形成等价关系。因此，后续分析仅需基于 X 轴方向的平面扩散模型进行，即可在保持主要物理特征的前提下显著简化计算与工程应用。

为了更直观地说明上述模型，可在坐标系中绘制两条关于 X 轴对称的曲线（或直线）：一条代表光在主传播方向上的近似运动轨迹，另一条表示其对应的扩散边界。若采用直线，则体现近场条件下的准直线发散；若采用曲线，则可反映边缘持续发散、扩散范围不断变化的真实过程。无论选取直线或曲线，这两条轨迹始终保持关于 X 轴对称，并在同一截面上界定光的分布范围，从而为后续的权重取值与能量近似计算提供清晰的几何依据。

三、扩散函数形式与物理含义（原说明保留）

光的扩散可采用如下基本函数形式表示：

text
f(x) = (K + N0) * x

其中，取发射口为坐标原点，沿主传播方向以 x 表示空间位置；K 为表征光源初始扩散特性的基础参数，N0 为与边界条件、光源尺寸及方向约束相关的常数修正项。二者共同构成扩散系数的线性组合，并通过与位置 x 的乘积，描述扩散角度或扩散范围随距离的线性增长。该形式既保留了传统线性扩散模型的简洁性，又通过 K 与 N0 的组合，为工程中不同光源类型及边界条件下的能量与流量近似计算提供了可调参数基础。

需要说明的是，光的扩散并非一个保持稳定宽度的直线传播过程，也不是简单的线性放大；它是一种边缘持续发散、截面逐步展开的过程。随着传播距离增加，扩散边界并非平行外移，而是以一定的发散规律改变形状与范围，使得扩散曲线表现为连续变化而非恒定斜率的直线。因此，任何以固定斜率或单一稳定直线来描述全程扩散的表达式，都只能在局部区间近似成立；更贴合实际的模型应当允许扩散参数（如 K、N0 等）随位置或角度条件发生受控变化，从而反映边缘不断发散的物理本质。

在实际测量中可以观察到，沿主传播方向上不同位置 x 处，所对应的扩散参数 (K+N0) 并非保持不变，而是随位置发生变化，即斜率处于不断变化的状态。这表明光的扩散过程在局部可用线性近似，但从整体上看，扩散斜率本身随 x 演化，需用变参数或分段描述来更接近真实情况。

由此可得，光在实际传播过程中表现为一种稳定但持续的分散与发散过程；本文所给的方程并不用来描述光“如何运动”或“如何传播”的动态机制，而是用于近似表示光在某一位置（或某一截面）上的分布状态（如扩散角度、方向权重、能量分布等）。该方程应理解为一种局部近似描述，而非全过程的运动方程。

需要强调的是，该方程仅反映光在经过一定传播后的“运动结果”，即在某一位置处的扩散与分布状态，而不直接给出光的动态传播过程。为了获得具体的能量或流量信息，还需通过额外的处理方式对光的权重进行取值。一般而言，靠近中心的区域权重较大，光能相对集中；而越接近边缘，权重逐渐减小，光能趋于分散与模糊。该方程本身不直接描述这一权重变化，而是通过与其他权重处理方法的结合，间接实现对不同位置能量分布的有效近似。

四、近场近似与“直线/趋势线”的含义（原说明保留）

在近光源区域，可取修正项 N0 = 0，此时扩散主要由参数 K 表征，且其变化可视为一种稳定、连续的演化过程。在这一范围内，光的扩散可近似为一种“准直线型”的发散运动，即：在局部区间内，扩散角度随距离近似呈线性增长，表现出类似直线运动的规律性。该近似简化了近场区的复杂边缘发散行为，为工程计算提供了一个可控且易于处理的起点。

需要特别说明的是，上述所绘制的直线仅用于表示光在外部观测或工程近似下的“运动趋势”或“有效传播方向”，并不代表光在截面上的实际空间排布。真实的分布表现为中间权重集中、边缘权重衰减且有持续发散特征的扩散场，而直线模型仅作为一种简化映射，用于描述扩散结果的整体走向，而非对光场内部结构的完整再现。

五、扩散斜率 (K+N0) 的工程取值与权重几何规则（原说明保留）

参数 (K+N0) 可理解为光在运动方向上某一点处的扩散角度斜率。其取值可通过该位置处光扩散的最大跨距（如截面直径或宽度）与光的运动距离之比来确定，即：

text
K + N0 = 最大扩散跨距 / 传播距离

具体取值时，先固定位置 X = X0，再在其截面上取从 Y = 0 到 Y = Y0 的垂直线（即 X = X0 的竖直线）；在该垂直线上任取一点，过此点作一条与 X 轴平行的直线，该平行线与扩散方程所对应的斜线（或曲线）相交，其相交关系即用于确定该点的权重大小。

从上述几何关系可以看出，在同一条垂直线 X = X0 上，所取点与原点连线的横向跨度越大，即该点越趋近于 Y = 0 的方向，对应的权重越大；反之，越远离中心、越靠近扩散边缘，权重越小。换言之，权重随该点在垂直线上的位置而变化，呈现出“近中心权重大、近边缘权重小”的典型分布特征，并与扩散角度及截面展宽形成一致的对应关系。

六、区间能量分配、权重本质与权重计算（原说明保留）

在实际计算中，应选取 Y0 的某个区间，而非单一位置，作为能量的统计与分配单元。该区间内的各点通过前述几何关系获得各自的权重，并在此基础上进行能量数值的汇总与分配，从而实现从局部点到区间的能量聚合。这种以区间为单位进行能量分配的方式，更符合工程中对光斑、截面或探测区域进行整体评估的需求，也有助于提高计算结果的稳定性与可操作性。

需要明确的是，此处定义的权重本质上是一种比例关系，表示局部能量相对于全局总能量的占比，而不是由单一几何面积或局部面积公式直接给出的绝对量。换言之，权重反映的是某一区间或位置在整体能量分布中所占的相对份额，其归一化对象为系统的总能量，从而确保模型输出的能量分配结果在全局尺度上保持自洽与守恒。

在模型实现上，权重比例可通过函数 f(x) 的反函数导数来表征。具体而言，取某一区间所对应的几何面积（即该区间内 f(x) 或其反函数导数所围成的区域），并将其除以整个函数在定义域内的总面积，所得比值即为该区间的权重比例。该方式将权重的计算转化为对函数覆盖区域的面积比，从而在保持模型几何直观性的同时，实现从局部特征到全局能量分配的定量映射。

随着 X 的增大，即沿光的主传播方向逐渐远离发射口，同一位置处的权重将呈现逐渐降低的趋势。这表明在整体能量分配中，近场区域占有较大比重，而远场区域由于扩散展宽与能量稀释，其相对权重相应减小，从而体现出光在稳定发散过程中能量由近及远的重新分配特征。

在光的边缘地带，当 X 较小时，由于扩散尚未充分展开，边缘区域权重接近于零；随着 X 增大，扩散范围逐步扩大，原本处于边缘的位置开始获得非零权重，并沿 Y 轴方向表现为从零逐渐增大的过程。这表明边缘能量并非在发射口立即存在，而是随传播距离增加、扩散角度展开而逐步参与整体能量分配，从而形成近场到远场的权重渐变结构。

七、权重计算示例（纯文本公式，f(x)=Kx 近场）

前提：

text
f(x) = K * x
K 为常数（近场先把 K 当常数）
x：沿 X 轴的距离

反函数

text
y = K * x
=> x = y / K
反函数：f^(-1)(y) = y / K

反函数导数

text
d(f^(-1)(y)) / dy = 1 / K

总面积（全局）

设最大传播距离 Xmax，对应 Ymax = K * Xmax：

text
A_total = ∫(0 → Ymax) (1/K) dy
        = (K * Xmax) / K
        = Xmax

局部面积（X = X0 处，Y0 = K * X0）

text
A_local(X0) = ∫(0 → Y0) (1/K) dy
            = Y0 / K
            = X0

权重比例（局部 / 全局）

text
W(X0) = A_local(X0) / A_total
      = X0 / Xmax
对，你这两个限定非常重要，而且是物理上必须写清楚的：

X 方向（传播距离）​

发射口为原点

光一旦发出，沿主传播方向的位置应满足

text
X > 0

（X=0 一般只表示发射面/出口位置，不参与“已传播”的分布计算）

“角度”指的是光的扩散角度，不是 K 的角度​

K 是斜率参数，用来近似/表示扩散角度的大小

真正物理量是：光的扩散半角（或全角）θ，且

text
0 < θ < 90°（半角）
0 < 全角 < 180°

在小角工程近似下，常用：

text
tanθ ≈ θ（弧度）
K ≈ tanθ ≈ θ
沿主传播方向的位置坐标满足 X > 0，其中 X=0 对应发射口位置。

本文中的“角度”指光的扩散角度（半角 θ 或全角），其物理范围一般为 0~180°（全角），对应半角 0~90°；参数 K 为该角度的工程近似表征（如 K≈tanθ），而非角度本身。
当光的扩散角度（全角）大于 180° 时，当前基于主传播方向 X 轴与局部斜率 K 的近似模型不再适用，此时应更换为其他模型，例如球面均匀扩散模型或其他能够描述大角度/近各向同性辐射的模型。该切换条件的引入，可避免将适用于有限方向性、具有一定传播主轴的扩散过程不恰当地外推至全向或近全向辐射情形，从而保证模型在不同光源类型与发散范围内的适用性界定清晰。
本模型不描述光的周期变化过程，但在一个周期（如半周期）内，扩散边界、能量权重与取值方法仍然适用；这并不构成对光的波动性或周期性的否认。
